# Owner feedback queue

This is a working queue, not a claim that a feature is live. Finish and verify one item before advancing. “In GitHub” means the code is pushed but the 4090 deployment has not been verified.

| Owner request | Current state | Evidence still needed |
| --- | --- | --- |
| Drop multiple files into the Inbox with minimal steps | Local implementation: one batch creates one Inbox item, with partial-upload reporting and link retry | Push, deploy, then upload mixed files from the live browser and reopen originals |
| Explain and open an apparently empty “Revision 3” card | Local implementation: Inbox cards open in Brain and explicitly distinguish an empty text preview from the original/attachments; version label no longer suggests content | Push, deploy, inspect the specific live note's source and attachments without assuming what is in it |
| Make the maths-test source text readable on a phone | Local implementation: full-width assessment row, collapsible details, sentence/label sections | Push, deploy, visually verify the maths-test record on the narrow live viewport |
| Calendar as a timed weekly grid, with current week indicator | In GitHub since `7c797c3`; not live-verified | Deploy and compare actual week/classes on phone and desktop |
| Color the class itself red for absence or yellow for lateness, including type | In GitHub since `d65a63c`; snapshot import now accepts typed attendance but automatic InSchool attendance extraction is not yet present | Deploy; verify linked attendance with real source records, then implement and live-test InSchool attendance collection |
| Automatically populate subjects, courses, lessons, assessments, assignments, attendance, grades and their details from InSchool/Teams | Partial: timetable one-week import live-verified; broader traversal and other categories incomplete. Snapshot preview/apply accepts source-linked attendance and grades, but no browser collection for them yet. Code now projects Microsoft Graph classes and assignments into School, with source-linked instructions, files and returned results available from the assignment card; not deployed or live-verified | Deploy and sync authorized Microsoft education classes/assignments, verify the visible cards and originals; reauthenticate the 4090's isolated school browser, verify multiweek import, then add missing InSchool datasets with coverage checks |
| Remove manual “cockpit” work from everyday use; commitments should come from capture/import | In GitHub: primary Tasks screen has no add forms; explicit Inbox task commands create source-linked tasks automatically, and inferred task notes offer one-step acceptance. Brain shows note text first and folds editing/correction controls away. Local implementation now extracts source-quoted return promises as reviewable candidates, but broad automatic promise creation is still missing | Push and deploy; live-test task/source linkage, Brain readability, and promise candidates; expand safe automatic extraction and simplify remaining surfaces |
| Norwegian/European date display and entry | In GitHub: reminders, assignment due times, assessment dates, attendance dates, grade dates, and goal dates use explicit `dd.mm.yyyy` entry; timestamps include 24-hour `hh:mm`. Calendar range and attendance summary now show Norwegian dates instead of raw ISO or the midnight-exclusive next day | Push and deploy; visually verify remaining date displays on the live phone browser |
| Whole-account Teams/365 and Gmail collection | Open/partial. Microsoft Graph now collects assignment, working, and submitted resource originals with provenance, plus each submission's outcome list. File downloads are bounded and missing/oversized or unreadable source data is reported as incomplete coverage. This is code-only and not live-account verified | Deploy, grant authorized scopes, and verify assignment instructions, files, submissions, published feedback/marks, pagination, and coverage against the signed-in school account; then continue the remaining Microsoft and Gmail datasets |

The live site may lag GitHub while remote deployment is unavailable. Do not mark a row finished from a passing unit test alone.
