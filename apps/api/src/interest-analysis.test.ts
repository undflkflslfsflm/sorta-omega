import{describe,expect,it}from"vitest";import{analyzeApprovedTopics}from"./interest-analysis.js";
describe("interest analysis",()=>{it("requires three distinct observations and explicit topic approval",()=>{const item=(id:string,topic:string)=>({id,actionKind:"saved",observedAt:null,metadata:{topic}});const result=analyzeApprovedTopics([item("1","AI"),item("2","AI"),item("3","AI"),item("4","Health")],["AI"]);expect(result.interests).toHaveLength(1);expect(result.interests[0]?.items).toHaveLength(3);expect(result.interests.some(value=>value.label==="Health")).toBe(false);});it("does not turn one observation into an interest",()=>{const result=analyzeApprovedTopics([{id:"1",actionKind:"liked",observedAt:null,metadata:{topic:"Photography"}}],["Photography"]);expect(result.interests).toEqual([]);expect(result.skippedSparseTopics).toEqual(["Photography"]);});it("blocks sensitive labels even when the owner includes them",()=>{const item=(id:string)=>({id,actionKind:"viewed",observedAt:null,metadata:{topic:"Political party preference"}});const result=analyzeApprovedTopics([item("1"),item("2"),item("3")],["Political party preference"]);expect(result.interests).toEqual([]);expect(result.blockedSensitiveTopics).toEqual(["Political party preference"]);});});
